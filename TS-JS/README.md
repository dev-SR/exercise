## Setup `pnpm`

```bash
npm install -g pnpm
# get store location
pnpm store path
# change default location (https://pnpm.io/configuring)
pnpm config set store-dir D:/.../pnpm_store/.pnpm-store

# Basic commands
pnpm install
pnpm add <pkg>
pnpm <cmd>
```
